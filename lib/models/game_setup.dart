import './team.dart';

class GameSetup {
  // a list of teams as they're created

  /// Add team {
  ///  take the team and add it to the game controller
  /// }
  void addTeam(Team team) {}

  /// Start Game {
  ///  set teams to the controller
  ///  start the game
  ///  }
  void startGame() {}
}
